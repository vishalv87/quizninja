{
  "name": "quizninja-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n api,ui -c blue,magenta \"cd api && air\" \"cd ui && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
